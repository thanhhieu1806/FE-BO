const { palette, brand, text, icon, border, surface, sidebar, navbar, chart } = require('./colors');
const { fontFamily, fontSize, fontWeight } = require('./typography');
const { layout, borderRadius, boxShadow, spacing } = require('./layout');

module.exports = {
  palette,
  brand,
  text,
  icon,
  border,
  surface,
  sidebar,
  navbar,
  chart,
  fontFamily,
  fontSize,
  fontWeight,
  layout,
  borderRadius,
  boxShadow,
  spacing,
};
